    <header>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
              <a class="navbar-brand" href="#">DESAFIO</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="#">Home</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/users">Lista de Usuários</a>
                  </li>
                  <li class="nav-item">
                      <a class="nav-link" aria-current="page" href="/register">Cadastrar</a>
                  </li>
                </ul>
              </div>
            </div>
        </nav>
    </header>

  {{#if deleted}}
    <div class="alert alert-success" role="alert">
        Usuário deletado com sucesso!
    </div>
{{/if}}
<div id="successMessage" style="display: none;" class="success-message">
    Conteúdo salvo com sucesso!
</div>
  <section id="usersList">
  <h2>Lista de Usuários</h2>
  <table class="table table-striped">
    <thead>
      <tr>
        <th scope="col">Id</th>
        <th scope="col">Nome</th>
        <th scope="col">Ver</th>
        <th scope="col">Delete</th>
      </tr>
    </thead>
    <tbody>
      {{#each users}}
        <tr>
          <td>{{id}}</td>
          <td>{{name}}</td>
          <td><a href="/users/{{id}}" class="btn btn-primary">Visualizar</a></td>
          <td><a href="/delete/{{id}}" class="btn btn-danger">Deletar</a></td>
        </tr>
      {{/each}}
    </tbody>
  </table>
  </section>